# json.extract! @user, :id, :username, :auth_token
# json.questions @user.questions do |q|
#   json.extract! q, :id, :user_id, :title, :body
# end
# json.answers @user.answers do |a|
#   json.extract! a, :id, :user_id, :question_id, :body
# end
